// The environment is an immutable host-provided snapshot. Entries retain their
// original NAME=VALUE spelling and order.
pub fn environment_count() -> u64 {
    return runtime_environment_count();
}

pub fn environment_entry(index: u64) -> string {
    let value: string = runtime_environment_entry(index);
    return value;
}
